<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sabzimart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".deliveredorder"></activity>
        <activity android:name=".updateproduct" />
        <activity android:name=".admin_opion" />
        <activity android:name=".orders" />
        <activity android:name=".conifirmorder" />
        <activity android:name=".cart" />
        <activity android:name=".productdetail" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Product_Set" />
        <activity android:name=".Add_Product" />
        <activity android:name=".Login" />
        <activity android:name=".Signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>